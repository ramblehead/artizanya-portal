query GetCharacter($episode: String!) {
  hero(episode: $episode) {
    name
    id
    appearsIn {
      title
    }
    friends {
      name
      id
      appearsIn {
        title
      }
    }
  }
}
